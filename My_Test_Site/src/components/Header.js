const Header = ({ ownerName }) => {
  return (
    <header>
      <h2>{ownerName}'s Test Site</h2>
      <p>Testing ReactJs syntax, Hooks, Forms and more...</p>
    </header>
  );
};

export default Header;
